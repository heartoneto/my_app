import { ILayoutProps } from "./Layout";

const base: ILayoutProps = {
    title: "Test",
    expandedMenu: true,
    activeKey: "a",
    children: undefined
};

export const mockBaseTemplateProps = {
    base,
};
